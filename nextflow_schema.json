{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/BfArM-MVH/GRZ_QC_Workflow/refs/heads/main/nextflow_schema.json",
    "title": "BfArM-MVH/GRZ_QC_Workflow pipeline parameters",
    "description": "Internal quality control pipeline for GRZs in the genomDE Model Project",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["outdir"],
            "anyOf": [{ "required": ["submission"] }, { "required": ["samplesheet"] }],
            "properties": {
                "submission": {
                    "type": "string",
                    "format": "directory-path",
                    "exists": true,
                    "description": "Path to the root folder of a submission. Can not be combined with --samplesheet."
                },
                "samplesheet": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "schema": "assets/schema_input.json",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to a comma-separated file containing information about the samples in a submission. Can not be combined with --submission."
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure."
                },
                "reference_path": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Folder to read and write genome reference files."
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "properties": {
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
